use std::io::Result;

pub fn init(server: &mut std::process::Child) -> Result<()> {
    // server
    //     .stdin
    //     .as_mut()
    //     .expect("failed to acquire stdin of server process");

    Ok(())
}

#[cfg(test)]
mod tests {}
